<div class="main-title">
  <div class="title">
    <p class="gamma"><%= t('.subtitle', number: @stage.number) %>: <%= @trip.title %></p>
    <h1><%= t('.title') %></h1>
  </div>
</div>

<%= render partial: 'form', locals: { trip: @trip, stage: @stage } %>
