<%= form_tag(user_to_dos_path(user), class: 'btn--form', data: { 'todo-create': true }, remote: true) do %>
  <%= hidden_field_tag :trip_id, trip_id %>
  <%= button_tag t('todos.create'), class: 'btn' %>
<% end %>
