<%= form_tag(user_to_do_path(current_user, current_user_to_do(trip.id)), class: 'btn--form', method: :patch, remote: true) do %>
  <%= hidden_field_tag :trip_id, trip.id %>
  <%= hidden_field_tag :transition, transition %>
  <%= button_tag update_todo_button_name(transition), class: "btn #{update_todo_button_class(transition)}" %>
<% end %>
