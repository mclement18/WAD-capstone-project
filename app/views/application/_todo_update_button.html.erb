<%= form_tag(user_to_do_path(user, todo), class: 'btn--form', method: :patch, remote: true) do %>
  <%= hidden_field_tag :transition, transition %>
  <%= button_tag todo_update_button_name(transition), class: "btn #{todo_update_button_class(transition)}" %>
<% end %>
