<% if @todo.saved_change_to_status? %>
  ToDo.respond(<%= @todo.trip_id %>, '', '', "You don't have any trip in your list.")
<% end %>

<% if flash.any? %>
  Alert.render('<%=j render 'alert_and_notice', alert: flash.alert, notice: flash.notice %>')
<% end %>
